---
layout: base.njk
pagination:
  data: collections.feelPages
  size: 1
  alias: feel
permalink: "/f/{{ feel.slug }}/"
eleventyComputed:
  title: "{{ feel.title }}"
  description: "{{ feel.description }}"
---
{#- A feel view. Mints no nav link, deliberately — same rule as tags. The only
    way here is by clicking the chip on a tile or a subject page, which is what
    makes the vocabulary self-teaching: you meet the words attached to a game
    you already have an opinion about, and the page is the definition.

    Membership is derived in eleventy.config.js by grouping subjects on `feel:`,
    with canon subjects excluded at the source — so no game in the ten ever
    appears here, and "comfort food" is a page about games that earn the
    description on their own rather than a footnote to the roster. -#}
{%- set view = feel %}
{%- set emptyLabel = "No games carry this note yet." %}
{% include "view.njk" %}
